
import prefixer from '../utils/prefixer';

const prefix = prefixer('REQUEST');

export const REQUESTING = prefix('REQUESTING');
export const SUCCESS = prefix('REQUEST');
export const FAILURE = prefix('REQUEST');
